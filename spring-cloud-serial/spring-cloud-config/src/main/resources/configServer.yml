server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fzx-wsss/spring-cloud
          searchPaths: 
  application:
    name: config-server